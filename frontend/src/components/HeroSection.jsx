const HeroSection = () => (
    <section className="hero">
        <h2>¡Haz la diferencia hoy!</h2>
        <p>Contribuye al cambio y ayuda a los demás.</p>
        <button onClick={() => alert('¡Vamos a donar!')}>Donar Ahora</button>
    </section>
);

export default HeroSection;
